import styled from 'styled-components/native';
import {TouchableWithoutFeedback, Text as RNText, Animated} from 'react-native';
// import {FontFamily, FontWeight, FontSize, LineHeight} from '../../helpers/typo';
// import {TextPT, ContainerPT} from './Button.types';

export const Container = styled(TouchableWithoutFeedback)``;

export const Text = styled(RNText)``;

export const Radio = styled(Animated.View)``;

export const Circle = styled(Animated.View)``;

export const Dot = styled(Animated.View)``;
